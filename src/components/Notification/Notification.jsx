import css from "./Notification.module.css"
const Notification = () => {
    return (
        <p className={css["notification"]}>There are no contacts yet, but you can add new one's!</p>
    )
}
export default Notification;